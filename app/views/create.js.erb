$(function(){
  $("#search_form").submit(function(event){
    event.preventDefault();
    $.ajax({
      type: "POST",
      url: $(this).attr('action'),
      data: "#user_input"
    });
  });
});

jQuery("#list_definitions").html("<%= escape_javascript(render('create')) %>");
